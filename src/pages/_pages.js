import homePage from "./home";
import login from "./login";
import register from "./register";

const pages = {
    home: homePage,
    login: login,
    register: register,
};

export default pages;
